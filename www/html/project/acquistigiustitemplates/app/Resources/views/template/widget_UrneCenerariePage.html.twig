<section class="widget_UrneCenerarie">
    <div class="structure_Container">
        <article class="widget-DetailFunebri">                
            <header>
                <h1>{{article.contentArticle.title}}</h1>
                {% if article.id < 48 %} 
                    <p>
                        {{article.contentArticle.subHeading}}
                    </p>
                {%endif%}
            </header>  
        
            
            <div class="body">
                {% if article.id < 48 and article.priorityImg is not empty %}
                    <img src="{{article.priorityImg.srcBig}}" alt="{{article.contentArticle.title}}" >                            
                {% endif %} 
                {{article.contentArticle.body | raw }}                    
            </div>
{#            <div id="disqus_thread"></div>#}
              
        {#<div class="right">            
            <ul class="anchors" data-anchors-news>
                <li data-anchor-action="back">-</li>
                {% for key,anchor in anchors %}
                    <li data-anchor-key="{{key+1}}"  class="item-anchor {% if loop.first is not empty %}active{%endif%}">
                        <a rel="nofollow" href="#cap-{{anchor|trim}}">{{anchor|replace({'-':' '})}}</a>
                    </li>
                {% endfor %}
                <li data-anchor-action="next">+</li>
            </ul>
            {% include versionSite ~ '/widget_ModelRanking.html.twig' %}           
        </div>#}
        
    </article>   
    </div>
</section>
{%if fetch_widget_AboutUs is defined %}
    {{ fetch_widget_AboutUs | raw  }}
{%endif%}